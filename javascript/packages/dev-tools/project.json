{
  "name": "@herb-tools/dev-tools",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "javascript/packages/dev-tools/src",
  "projectType": "library",
  "targets": {
    "build": {
      "outputs": ["{projectRoot}/dist"],
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "dependsOn": [
        "@herb-tools/core:build",
        "@herb-tools/client:build",
        "@herb-tools/highlighter:build",
        "@herb-tools/browser:build"
      ]
    },
    "dev": {
      "executor": "nx:run-script",
      "options": {
        "script": "dev"
      }
    },
    "test": {
      "executor": "nx:run-script",
      "options": {
        "script": "test"
      }
    },
    "clean": {
      "executor": "nx:run-script",
      "options": {
        "script": "clean"
      }
    }
  },
  "tags": []
}
