{
  "name": "@herb-tools/config",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "javascript/packages/config/src",
  "projectType": "library",
  "targets": {
    "build": {
      "inputs": [
        "default",
        "{workspaceRoot}/lib/herb/defaults.yml"
      ],
      "outputs": ["{projectRoot}/dist", "{projectRoot}/tsconfig.tsbuildinfo"],
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "dependsOn": [
        "@herb-tools/core:build"
      ]
    },
    "test": {
      "executor": "nx:run-script",
      "options": {
        "script": "test"
      }
    },
    "clean": {
      "executor": "nx:run-script",
      "options": {
        "script": "clean"
      }
    },
    "dev": {
      "executor": "nx:run-script",
      "options": {
        "script": "dev"
      }
    }
  },
  "tags": []
}
