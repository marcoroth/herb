#ifndef HERB_JNI_NODES_H
#define HERB_JNI_NODES_H

#include <jni.h>

#include "../../src/include/ast/ast_nodes.h"
#include "../../src/include/parser/parser.h"

#ifdef __cplusplus
extern "C" {
#endif

<%- nodes.each do |node| -%>
jobject Create<%= node.name %>(JNIEnv* env, <%= node.struct_type %>* node, const parser_options_T* options);
<%- end -%>

jobject CreateErrorNode(JNIEnv* env, AST_NODE_T* error_node);
jobject CreateASTNode(JNIEnv* env, AST_NODE_T* node, const parser_options_T* options);

#ifdef __cplusplus
}
#endif

#endif
