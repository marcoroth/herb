---
source: "Engine::Slots::VisitorTest#test_0021_falls back to index and suggests a wrapper when a collection body has several roots"
input: "<% @u.each do |u| %><li>a</li><li>b</li><% end %>"
options: {filename: "app/views/test.html.erb"}
---
file: app/views/test.html.erb
version: b3fe409f

slots:
  index=0  type=collection  node_path=[0]  expression="@u.each do |u|"  key_source=index

warnings:
  type=unkeyed_collection  tag_name=nil  message="Add a `<%# herb:key ... %>` directive to this collection, or wrap each row in a single element with a `herb-key` or `id` attribute, so rows can be matched across updates. Without a key, inserting or reordering the collection re-renders every following row and discards its focus, scroll, and input state."
