---
source: "Engine::ErrorHandlingTest#test_0022_invalid erb control flow structure - else outside scope"
input: |2-
<div
  <% if some_condition %>
    class="a"
>
  <% else %>
  <% end %>
</div>
options: {validators: true}
---
[source]:2:3: `<% if %>` started here but never closed with an end tag. The end tag may be in a different scope. (and 2 more errors) (Herb::Engine::ParseError)

✘ [MissingERBEndTag] `<% if %>` started here but never closed with an end tag. The end tag may be in a different scope.

    [source]:2:3:
      2 │   <% if some_condition %>
        ╵   ~~~~~~~~~~~~~~~~~~~~~~~

✘ [ERBControlFlowScope] `<% else %>` appears outside its control flow block. Keep ERB control flow statements together within the same HTML scope (tag, attribute, or content).

    [source]:5:3:
      5 │   <% else %>
        ╵   ~~~~~~~~~~

✘ [ERBControlFlowScope] `<% end %>` appears outside its control flow block. Keep ERB control flow statements together within the same HTML scope (tag, attribute, or content).

    [source]:6:3:
      6 │   <% end %>
        ╵   ~~~~~~~~~
