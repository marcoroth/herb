---
source: "Engine::ErrorHandlingTest#test_0012_error with source context"
input: |2-
<div>
  <h1>Working title</h1>
  <section>Text content
</wrong_tag>
options: {validators: true}
---
[source]:1:1: Opening tag `<div>` at (1:1) doesn't have a matching closing tag `</div>` in the same scope. (and 2 more errors) (Herb::Engine::ParseError)

✘ [MissingClosingTagError] Opening tag `<div>` at (1:1) doesn't have a matching closing tag `</div>` in the same scope.

    [source]:1:1:
      1 │ <div>
        ╵ ~~~~~

  Add the closing tag, or make it self-closing.

✘ [MissingClosingTagError] Opening tag `<section>` at (3:3) doesn't have a matching closing tag `</section>` in the same scope.

    [source]:3:3:
      3 │   <section>Text content
        ╵   ~~~~~~~~~

  Add the closing tag, or make it self-closing.

✘ [MissingOpeningTagError] Found closing tag `</wrong_tag>` at (4:2) without a matching opening tag in the same scope.

    [source]:4:1:
      4 │ </wrong_tag>
        ╵ ~~~~~~~~~~~~

  Add the matching opening tag, or remove this closing tag.
