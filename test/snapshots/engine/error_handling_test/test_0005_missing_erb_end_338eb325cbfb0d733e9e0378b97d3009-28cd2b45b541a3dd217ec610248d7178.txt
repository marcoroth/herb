---
source: "Engine::ErrorHandlingTest#test_0005_missing erb end"
input: |2-
<div>
  <% if user.active? %>
    <span>Active</span>
  <!-- Missing end! -->
</div>
options: {validators: true}
---
[source]:5:7: unexpected_token_close_context: unexpected end-of-input, assuming it is closing the parent top level context (and 4 more errors) (Herb::Engine::ParseError)

✘ [RubyParseError] unexpected_token_close_context: unexpected end-of-input, assuming it is closing the parent top level context

    [source]:5:7:
      5 │ </div>
        ╵       ~

  Check the Ruby syntax inside the ERB tag.

✘ [RubyParseError] conditional_term: expected an `end` to close the conditional clause

    [source]:2:6:
      2 │   <% if user.active? %>
        ╵      ~~

  Check the Ruby syntax inside the ERB tag.

✘ [MissingClosingTagError] Opening tag `<div>` at (1:1) doesn't have a matching closing tag `</div>` in the same scope.

    [source]:1:1:
      1 │ <div>
        ╵ ~~~~~

  Add the closing tag, or make it self-closing.

✘ [MissingERBEndTagError] `<% if %>` started here but never closed with an end tag. The end tag may be in a different scope.

    [source]:2:3:
      2 │   <% if user.active? %>
        ╵   ~~~~~~~~~~~~~~~~~~~~~

✘ [MissingOpeningTagError] Found closing tag `</div>` at (5:2) without a matching opening tag in the same scope.

    [source]:5:1:
      5 │ </div>
        ╵ ~~~~~~

  Add the matching opening tag, or remove this closing tag.
